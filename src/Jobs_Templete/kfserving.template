apiVersion: "serving.kubeflow.org/v1alpha2"
kind: "InferenceService"
metadata:
  name: {{ job["model_name"] }}
spec:
  default:
    predictor:
      {{ job["framework"] }}:
        storageUri: "file://{{ job["model_base_path"] }}"